language: node_js
node_js:
  - 'stable'
cache:
  directories:
    - node_modules

before_install:
  - cd ts2
#  - cd exam
script:
  # - npm run lint
  # - npm run test
  - npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $repo_token
  local_dir: build
  on:
    branch:
      - master
      - devel
      # - elasticsearch_support
      - frontend_lesson_2_7
